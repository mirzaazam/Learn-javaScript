// add to string in oneline

var first_string = "I am going to be";
console.log('First string is = ',first_string);
var second_string = "an awesome web developer";
console.log('Second string is = ',second_string);

//combine all string
var combine = first_string + (' ') + second_string;
console.log('\n Combine line is =',combine);